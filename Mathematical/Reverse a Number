#include <iostream>
using namespace std;

int main() {
    // Write C++ code here
    
    int n=23456;
    int v=0;
    while(n>0)
    {
        int a=n%10;
        // cout<<a;
        n=n/10;
        // cout<<n;
        v=v*10+a;
    }
    
    cout<<v;

    return 0;
}
